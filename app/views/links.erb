<h1> Title </h1>
  <ul id=links>
    <% @links.each do | l | %>
      <li> Title: <%= l.title %>
          url: <%= l.url %>
      </li>
    <% end %>
  </ul>

  <form action= '/links/new' method='get'>
    <input type= 'submit' value='New bookmark' />
  </form>
